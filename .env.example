# Server
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Owner dashboard login (change in production!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=bacco2026
# Or use a bcrypt hash instead of ADMIN_PASSWORD:
# ADMIN_PASSWORD_HASH=

# Required in production so sessions survive restarts
JWT_SECRET=change-this-to-a-long-random-string

# Database (placeholder)
# DATABASE_URL=postgresql://user:pass@localhost:5432/bacco_perbacco

# Railway: JSON data and uploads live on the container disk unless you attach a volume
# for backend/data and public/uploads.
